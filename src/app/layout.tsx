import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    template: "%s | The Challenge Tennis",
    default: "Court Planner | The Challenge Tennis",
  },
  description: "View tennis court availability and schedules for The Challenge Tennis Club.",
  icons: {
    icon: "/favicon.ico",
  },
  openGraph: {
    title: "Court Planner | The Challenge Tennis",
    description: "View tennis court availability and schedules for The Challenge Tennis Club.",
    url: "https://court-planner.vercel.app", // Adjust if you have a custom domain
    siteName: "Court Planner",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body suppressHydrationWarning
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
